#include <iostream>
#include <unordered_map>

/*
Criterias:
1) Deterministic
    The same input always produces the same has value
2) Distribute values uniformly
3) Fast 
    Compute the hash value efficiently
*/

int main() {
    return 0;
}